global:
  scrape_interval: 10s

rule_files:
  - /etc/prometheus/alertes.yml

scrape_configs:
  - job_name: fastapi-rag
    metrics_path: /metrics
    static_configs:
      - targets:
          - host.docker.internal:8000

